[tool.poetry]
name = "mediawiki-scraper"
version = "0.0.0"
description = "MediaWiki Scraper"
authors = ["Elsie Hupp <github@elsiehupp.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
