[build-system]
requires = [
    "setuptools>=54",
    "wheel",
    "pandas>=1.1.5", 
    "numpy>=1.19.5"]
build-backend = "setuptools.build_meta"
[tool.poetry]
readme = "README.md"
description = "Solving **MULTI-CRITERIA DECISIONS PROBLEMS** with python."