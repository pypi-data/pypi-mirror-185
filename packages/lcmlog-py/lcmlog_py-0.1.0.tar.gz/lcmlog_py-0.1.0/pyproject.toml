[tool.poetry]
name = "lcmlog-py"
version = "0.1.0"
description = "LCM log utilities"
authors = ["Kevin Barnard <kbarnard@mbari.org>"]
license = "MIT"
readme = "README.md"
packages = [{include = "lcmlog"}]

[tool.poetry.dependencies]
python = "^3.7"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
