- simplify code structure a bit, remove a function

Features:
    - Add a label that show total file and folder count as well as total used space of
    the start directory

Bugfixes:
    - deletion of items from the global item containers
    - with that size update of directories in global dir container
    - childless nodes deletion from global item lists