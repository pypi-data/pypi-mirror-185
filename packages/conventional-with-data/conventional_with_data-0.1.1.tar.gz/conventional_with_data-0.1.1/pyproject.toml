[tool.poetry]
name = "conventional_with_data"
version = "0.1.1"
description = ""
authors = ["Shay Hill <shay_public@hotmail.com>"]
readme = "README.md"

packages = [
    { include = "cz_conventional_with_data.py" },
    { include = "questions.py" },
]

[tool.poetry.dependencies]
python = "^3.8"
commitizen = "^2.39.1"

[tool.poetry.group.dev.dependencies]
pytest = "^7.2.0"
pytest-mock = "^3.10.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
