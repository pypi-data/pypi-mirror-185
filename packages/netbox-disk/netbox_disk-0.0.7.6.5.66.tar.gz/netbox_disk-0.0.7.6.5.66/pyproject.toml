[tool.poetry]
name = "netbox-disk"
version = "0.0.7.6.5.66"
description = "Netbox Disk Plugin"
authors = [
    "Tim Rhomberg <timrhomberg@hotmail.com>",
]
readme = "README.md"
packages = [{include = "netbox_storage"}]

[tool.poetry.dependencies]
python = "^3.7"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"