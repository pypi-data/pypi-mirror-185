#!/bin/bash
#  These are the shortcust for various deployments - uncomment, or use as appropriate

# e.g. ensure all pipfile packages are including isn requires:
# pipenv run python ./scripts/pipfile_to_config.py

# Note: this is a template for a local bump / build / deploy
# $1 must be in [patch, minor, major]
# $2 must be a release message, optional other than for major, minor
# if [ -z "$2" ]; then
#     pipenv run version-update $1
# else
#     pipenv run version-update $1 -m "$2"
# fi


# deploy as package3
# rm -rf dist
# pipenv run python -m build --sdist --wheel .
# pipenv run python -m twine upload --verbose --repository gitlab dist/*